## Verlauf

# Enironment Datei anlegen und den APIUrl eintragen

1. cd /angular_frontend/
2. mkdir environments
3. im Ordner environments eine Datei environment.ts anlegen
4. im Ordner environments die Daten aus der environment.example.ts nach environment.ts kopieren
